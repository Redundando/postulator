# `jsonLd` — [Metadata] JSON+LD

A content type to create Json LD in the home page, posts and ui sections (posts)

**Display field:** `label`

## Fields

| Field ID | Type | Required | Default | Validations |
|----------|------|----------|---------|-------------|
| `label` | Symbol |  |  |  |
| `option` | Symbol | ✓ |  | one of: `Article`, `Item List`, `Organization`, `Custom` |
| `json` | Object | ✓ |  |  |
